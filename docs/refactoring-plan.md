# Refactoring plan (v3 / `develop`)

Living document. Owned by the `/refactor-module` skill: pick an item, do it with characterization tests, tick it, add what you discovered. Items are ordered by (risk removed × how many other items they unblock). IDs are referenced from `.claude/skills/*`.

Audit date: 2026-09-19. State of the code base then: ~28 k lines of Java in the reactor, 7 test classes (~1.1 k lines) all in `fluxcord-core`, no CI job running tests, no `@Deprecated` API, 20 broad `catch (Exception)` in `PluginManager` alone.

## 0. Safety net first

- [x] **T0 — CI runs the tests.** (2026-09-20) `.github/workflows/ci.yml`: `mvn -B -ntp verify` on push/PR to `develop`/`main` (Dependabot PRs included), Surefire reports uploaded on failure, JaCoCo 0.8.15 report-only in the root pom (core coverage at start: 9 %). First run surfaced a real race in `FileDataStorage.close()` vs the debounced background save (data loss at shutdown), fixed in the same batch.
- [x] **T1 — Test fixtures for the engine.** Done 2026-09-20 (`core/testing/PluginJars` + `FixturePlugin` + `PluginCalls`, `PluginManagerTest` 11 tests; fixed reload id/name key and class-loader leak on failed load). A `FakePlugin` (in-memory `Plugin` + `PluginDescriptor`) and a helper that builds a real plugin jar in a `@TempDir` (plugin.yml + compiled class) so `PluginManager` can be tested end-to-end. Unblocks P1/P2/E2.
- [x] **T2 — Characterization tests for the pure classes.** Done 2026-09-20 (low level): `Debouncer`, `YamlConfiguration`, `EnvAwareYamlConfiguration`, `PluginDescriptor`, `DependencyResolver`, `PluginClassLoader`, `HealthServer` (41 → 91 tests). Found and fixed: resolver returned the load order **reversed** (dependants before dependencies) and non-deterministic; `Debouncer.cancelAndAwait` did not actually wait for a running action (`FutureTask.cancel` reports success while running). `SimpleEventManager` (22), `SimpleCommandRegistry` (11, found: alias removal stole other commands' aliases), `SimpleLanguageManager` (16), `AbstractDataStorage`+`FileDataStorage` (22, found: cold-scope `set` wiped the rest of the scope on the next save, last-key removal never persisted, corrupt file crashed every access), `SimplePermissionManager` (12, found: cached defaults survived re-registration), `AudioPipeline` (13, found: fade one frame late) — all done 2026-09-20, 187 core tests. **T2 complete.**

## 1. Plugin lifecycle & identity (highest impact)

- [x] **P1 — One lifecycle path.** Done 2026-09-20: the phased boot/shutdown methods and `enablePlugin`/`disablePlugin` share `markEnabled`, `markDisabled` and `releaseResources` (events, permissions, audio, commands); the phased shutdown used to leak everything but event listeners. `cleanupResources` still releases plugins stuck in ERROR. Both paths remain (boot needs the pre-connect/post-connect split) but no longer diverge.
- [x] **P2 — Identity = `id` everywhere.** Done 2026-09-20: `PluginManager.reloadPlugin`, `AudioServiceImpl.pluginGuilds` and `AudioPipeline` handler maps are keyed by `getId()`; remaining `getName()` uses are display only. Open: should ids be forced lowercase (only the language namespace lowercases)? Convention so far: lowercase ids in plugin.yml.
- [x] **P3 — Classloader hardening.** (2026-09-20) `fr.farmvivi.fluxcord.api` added to `CORE_PACKAGES`; `getResourceAsStream` no longer uses the JarURLConnection cache (kept the jar locked on Windows after `close()`, blocking reload); template scope fixed. Left: fail fast in `loadPlugin` when a jar bundles api classes (now harmless: parent-first), and closing the classloader on every failure path (P1).
- [x] **P4 — Dependants of failed plugins.** Done 2026-09-20: `PluginManager.markFailed(id)` (load or any enable phase) sets `ERROR`, releases what the plugin registered, and fails every plugin that hard-depends on it, transitively (soft dependants untouched); `getFailedPlugins()` exposes the set. `PluginManagerTest` +2.
- [x] **P5 — `PluginContext` exposes plugin-scoped adapters.** Done 2026-09-20: `getCommands()`, `getPermissions()`, `getLanguage()`, `getStorage()`, `getBinaryStorage()` on `PluginContext` (built by `PluginManager` from the descriptor id/name, before `onLoad`); `AbstractPlugin` delegates to them under the same names (`getCommands()` …); the old `getPluginXxx()` getters, the shared-manager fields of `AbstractPlugin` and the shared-manager getters of `PluginContext` were removed the same day (user decision: no legacy paths). Adapters gained id-based constructors and the delegates plugins needed (`getDefaultLocale`, cooldown queries).

## 2. Boot & permissions

- [x] **B1 — Operators.** Done 2026-09-20: `permissions.operators` in config.yml, guild owner/ADMINISTRATOR mapping via a resolver installed after connect, `perm set|unset|list|nodes` command for stored per-user overrides (`/op` dropped on 2026-09-20: operators are file + Discord roles only); `shutdown` gated on operator status. Defaults evaluated live, only stored overrides cached.
- [x] **B2 — `Fluxcord` bootstrapper → instance.** Done 2026-09-20: `core/FluxcordRuntime(baseDir, config, discordAPI)` wires the services, `start()`/`stop()` (once, never throws)/`requestShutdown()`/`awaitShutdownRequest()`/`startHealthServer(port)`; `Fluxcord.main` is ~60 lines and the only place with `System.exit`; the static getters are gone. `ShutdownCommand` gets a `Runnable` (`SimpleCommandService.setShutdownHandler`). `FluxcordRuntimeTest` (7) boots the engine with a fixture plugin and a mocked `DiscordAPI`, including a failed connect and a restart reading back stored data.
- [x] **B3 — Configuration typing.** Done 2026-09-20: `core/config/CoreSettings` (records `Commands`, `DataStorage`+`Database`, `BinaryStorage`+`S3`, plus `AudioSettings`) parsed and validated once by `CoreSettings.from(config, baseDir)`; `FluxcordRuntime`, `StorageFactory`, `BinaryStorageFactory`, `DatabaseDataStorage`, `SimpleCommandService` take the record they need — no string key outside `CoreSettings` (and the plugin-side `PluginConfiguration`). Relative storage folders resolve against the base dir. The global `setPrefix` no longer rewrites `config.yml` (comment-loss hygiene item: closed for the core). `CoreSettingsTest` (6).

## 3. Commands

- [x] **C1 — Split `SimpleCommandService`.** Done 2026-09-20 (917 → ~600 lines): `CommandExecutor` (gating pipeline, cooldowns, metrics, events; `CommandExecutionTest`) and `SlashCommandDataMapper` (command model → JDA `CommandData`; `SlashCommandDataMapperTest`). Sync/debounce and prefix storage stay in the service (small, and part of the `CommandService` contract). Bug fixed on the way: subcommand options lost their choices/bounds/autocomplete on sync.
- [x] **C2 — Parser loop semantics.** Done 2026-09-20: the first parser that recognises the event owns it (`dispatch`). Unknown command → debug log only (a `!foo` message that is not ours must stay silent); parse failure → ephemeral `commands.messages.parse_error` reply on the invoking transport; refused or failed execution → error reply on every transport (before, a slash command refused by permission/cooldown/guild-only got **no reply at all** — "The application did not respond").
- [x] **C3 — Autocomplete.** Done 2026-09-20: `CommandListener.onCommandAutoCompleteInteraction` → `SimpleCommandService.handleAutocomplete` (≤ 25 choices, failures answer empty); API `AutocompleteProvider<T>` + `AutocompleteContext(partial, guildId, userId, options)`, legacy `Function<String, …>` adapted; `/perm` uses it for permission nodes and a real `userOption`; console parses USER options by ID. `CommandAutocompleteTest` (5).
- [x] **C4 — Command namespace collisions.** Decided 2026-09-20: names stay global (a Discord slash name is unique per bot); a second registration — or a name that is already another command's alias — is refused with an ERROR log naming both owners, and `registerCommand` returns false. No automatic prefixing.
- [x] **C5 — `CommandMessageBuilder`.** Done 2026-09-20: `core/command/reply/ReplyTarget` (`of(event)`) with `InteractionReplyTarget` (initial reply / `deferReply` / edit of the placeholder / zero-width placeholder deleted when empty), `MessageReplyTarget` (reply to the message, ephemeral emulated by deleting the reply and, with MESSAGE_MANAGE, the trigger after 1 min) and `ConsoleReplyTarget` (`[CONSOLE]` rendering, `render()` static). The builder (613 → ~180 lines) only composes within Discord's limits; the never-true "building took > 2.5 s" fallback and the unused `InteractionCommandContext` are gone. `CommandMessageBuilderTest` (8) pins the transport behaviours.
- [x] **C6 — Subcommands are routed.** Done 2026-09-20: `CommandParser.selectSubcommand` (name or alias) used by the three parsers (slash: `getSubcommandName()`; text/console: next token), `dispatch` executes `context.getCommand()`; `CommandExecutor` inherits permission/guild-only from ancestors and keys cooldowns by `getFullName()`. `SimpleCommandBuilder` now really links subcommands to their parent (`SimpleCommand.ParentLink` — the old code built a copy and threw it away, so `getParent()` was always null). `SubcommandRoutingTest` (7).

## 4. Events

- [x] **E1 — Dispatch semantics.** Done 2026-09-20: dispatch is polymorphic (concrete class first, then supertypes, per priority); `ignoreCancelled` now has Bukkit semantics (`true` = skipped once cancelled, default `false` = always called); handler lists are copy-on-write (registration from the main thread raced with `fireEvent` from storage/async threads); priority Javadoc fixed (LOWEST → MONITOR). Open: virtual threads for `fireEventAsync`.
- [x] **E2 — JDA listener ergonomics.** Done 2026-09-20: `DiscordAPI.addEventListeners(plugin, ...)`/`removeEventListeners(plugin)` (builder + live JDA, tracked per plugin id, released by `PluginManager.releaseResources`), `AbstractPlugin.addDiscordListeners(...)`; music/example/ai-audio plugins and the template use it; docs (`plugin-development`, `core-features`, `template-quickstart`, READMEs) rewritten — `@EventHandler` is documented as Fluxcord-events-only.

## 5. Storage

- [x] **S1 — Managers as interfaces.** Done 2026-09-20: `DataStorageManager` / `BinaryStorageManager` are api interfaces, implemented by `core/storage/SimpleDataStorageManager` and `core/storage/binary/SimpleBinaryStorageManager` (created by the factories).
- [x] **S2 — Collapse the scoped views.** Done 2026-09-20: the 16 view classes are replaced by `api/storage/ScopedStorage` and `api/storage/binary/ScopedBinaryStorage` (scope + optional namespace prefix via `namespaced(id)`; `StorageKey.*Scope()` factories own the scope formats). Storage layout unchanged (`ScopedStorageTest`). API break: plugins that named `PluginGuildStorage` & co now use `ScopedStorage`.
- [x] **S3 — Shared Gson configuration.** Done 2026-09-20: `core/storage/StorageJson` (`compact()`/`pretty()`/`convert()`; `java.time` as ISO-8601, integral numbers read as `Long`, no HTML escaping) used by both backends; supported value types documented in `docs/core-features.md`; `StorageJsonTest`.
- [x] **S4 — DB tests.** Done 2026-09-20 with H2 (test scope) in MySQL mode: `DatabaseDataStorageTest` (upsert, scopes, JSON payloads, table prefix) through a new package-private `DatabaseDataStorage(DataSource, SqlDialect, prefix, events)` constructor. H2 cannot emulate PostgreSQL's `ON CONFLICT ... DO UPDATE`; the PostgreSQL upsert stays covered by `SqlDialectTest` only — a real-server check belongs to the in-cluster dev bot.

## 6. i18n

- [x] **L1 — Collapse the lookup cascade.** Done 2026-09-20: `SimpleLanguageManager` is 200 lines with one map (`namespace → locale → key`), one `candidates()` list (requested → same-language variant, the configured default first → default → en-US) and one `StringRetrievalEvent` per lookup (was fired twice with args). `LanguageFiles` is the single YAML loader (bundled resources, `lang/` folders, plugin jars) — three copies of the flattener are gone, `LanguageFileLoader` deleted. Namespaces are case-insensitive: the adapter used the plugin id verbatim while `PluginManager` lower-cased it, so a plugin with an upper-case id could never find its own strings. `LanguageFilesTest` (4), `SimpleLanguageManagerTest` 17, `PluginManagerTest` +1.

## 7. Audio

- [x] **A1 — Key by plugin id** (done with P2, 2026-09-20).
- [x] **A2 — Extract the send strategy** (done 2026-09-20: `SendStrategy`, one-pass BE mixer, `AudioSettings` fade/ducking, frame event opt-in) (bypass vs mix, Opus rejection, priority fades) from `AudioPipeline` into a testable class; translate the French comments while there. Decided 2026-09-20: bypass now applies volume × fade in its existing LE→BE pass (no extra pass; Opus untouched).
- [~] **A3 — `ai-audio-plugin`.** Decision 2026-09-20: keep it in the reactor. It is the placeholder for a future voice AI plugin (listens to the voice channel and answers by voice in given scenarios; needs a fast voice-to-voice model). Nothing to do until that work starts.

## 8. Hygiene (do opportunistically inside the chantiers above, never as drive-by commits)

- Broad `catch (Exception)` → catch what the call can throw; never swallow without logging the cause.
- Null-checks on always-injected services (`if (eventManager != null)` ×20 in `PluginManager`) → make the constructor require them.
- French/English mix in comments and logs: new text in English; rewrite only what you touch.
- ~~`sonar-project.properties` says `sonar.java.source=17` while the build targets 25.~~ Fixed 2026-09-20; the Sonar workflow now also runs the tests so coverage reaches SonarCloud.
- The `Deploy` webhook step in `.github/workflows/docker-image-ci.yml` (and the `DEPLOY_WEBHOOK_URL` secret) is a leftover of the v2 VPS setup; deployments are now handled outside this repo. Remove the step.
- `docs/` describe intended behaviour; after each chantier update the relevant page.

### Found in the field
- [~] **M1 — YouTube playback.** `youtube-source 1.18.2` (latest release, 2026-07) is broken by YouTube-side changes (issues #226 #236 #240; IOS client version, TV client, cipher). Fixed on master but unreleased (#244). 2026-09-20: pinned the per-commit snapshot `2be8e542…-SNAPSHOT` (2026-09-17) from `maven.lavalink.dev/snapshots` — playback works again through the fallback clients, but the first attempt still logs `Must find sig function` for player `4fd832e7` (noisy, not fatal). **Switch back to a release as soon as 1.18.3 exists**; consider `remote poToken` (PR #229) if "Sign in to confirm" comes back.

## Decisions log

| Date | Decision | By |
|---|---|---|
| 2026-09-19 | Plan created; skills + Stop hook introduced; first chantier to be chosen after review. | user + Claude |
| 2026-09-20 | T0 done as a dedicated workflow + JaCoCo report-only (no threshold), CI also on Dependabot PRs. Keep working on `develop`; branches optional. | user |
| 2026-09-20 | E1: polymorphic dispatch and Bukkit `ignoreCancelled` semantics (API behaviour change; no plugin in the repo used `ignoreCancelled`). | user |
| 2026-09-20 | i18n fallback: configured default locale before en-US. Audio: lone PCM source keeps the bypass path but gets volume/fade applied in the byte-swap pass. | user |
| 2026-09-20 | B1: operators = config list + guild owner/ADMINISTRATOR (no `op` command); `perm` command manages stored overrides. Ducking default 20 %. | user |
| 2026-09-20 | S1/S2: break the api cleanly (no @Deprecated shims) — `ScopedStorage`/`ScopedBinaryStorage` replace the 16 scoped views; S4 will use H2 (test scope). | user |
| 2026-09-20 | C4: command names stay global; collisions are refused loudly (error log naming both owners), no per-plugin prefixing. | user |
| 2026-09-20 | P5 follow-up: plugins go through `PluginContext`/`AbstractPlugin` scoped views only; legacy getters and shared-manager accessors removed (API break, no stable v3 yet). | user |
| 2026-09-20 | `OptionType2` renamed to `OptionType` (api break, plugins replace the import). `ai-audio-plugin` stays as the seed of a future voice-to-voice AI plugin. | user |

## 8. Coverage pass (2026-09-20, driven by the JaCoCo report — same numbers as SonarCloud)

- [x] **S3BinaryStorage** (0 % → tested with a mocked `S3Client`/`S3Presigner` through a package-private constructor). **Bug fixed**: uploads never worked — `RequestBody.fromInputStream(stream, -1)` throws "Content-length must not be negative" on a background thread while `saveFile` answered `true`. The output stream now buffers and uploads on `close()`, and a failed upload makes `saveFile` return `false`. `S3BinaryStorageTest` (13).
- [x] **FileBinaryStorage / AbstractBinaryStorage** (`FileBinaryStorageTest`, 6: layout, overwrite, download, delete, listing, content types, event vetoes). Layout change: scope directories are `user/1` instead of `user:1` (a colon is not a valid file name on Windows); legacy folders are renamed once at startup.
- [x] **TextCommandParser** (`TextCommandParserTest`, 9). Improvement: a trailing STRING option takes the rest of the line (`!play never gonna give you up` is one query; quotes no longer needed).
- [x] `EventRegistryImpl` deleted (unused, 0 % coverage).
- [x] **HelpCommand** (`HelpCommandTest`, 6, through the real service on the console transport). Fixes: the general description was hardcoded English (now the `commands.help.general_description` key with the transport's prefix as `{0}`); `help Music` from text/console now gives the category help (positional options made the category unreachable); category names keep their spelling in autocomplete (`getCategories()` no longer lower-cases).
- [x] **PermCommand** (`PermCommandTest`, 4). Fix: `perm list <userId>` from text/console put the id in the `permission` slot and always answered with the usage.
- [x] **ConsoleCommandParser + SimpleCommandContext** (`ConsoleCommandParserTest` 7, `SimpleCommandContextTest` 4). `PositionalArguments` now holds the argument splitting/assignment shared by the text and console parsers (was duplicated). Fixes: an optional option whose token did not parse used to swallow every following token (later options starved) — the token now goes to the next option; `validateOptions` did not check the Java type against the option type (a `String` in an INTEGER option passed); `deferReply()` reset the ephemeral flag.
- [x] **AudioServiceImpl** (`AudioServiceImplTest` +3: receive handlers, guild close, null guards). Fix: deregistering a plugin's send handler forgot the guild for that plugin even when its receive handler was still registered there, so `closeAllConnectionsForPlugin` (plugin disable/reload) leaked the receive handler.
- [x] **SimpleCommandBuilder** (`SimpleCommandBuilderTest`, 5: every option type, constraints, metadata defaults, subcommand links, build rules). No bug.
- [x] **PluginConfiguration** migration paths (`PluginConfigurationTest` +4: migration class, plugin as migrator, newer file, failing migration/validation, backup). No bug.
- [x] **ConsoleCommandService** (`ConsoleCommandServiceTest`, 3, through an `InputStream` constructor seam). No bug.
- [x] **CoreConfiguration** edge cases (`CoreConfigurationTest` +4: missing folder, newer file, legacy migration with backup, malformed YAML). Fix: a syntax error in `config.yml` escaped as a raw snakeyaml `ParserException`; `YamlConfiguration.reload` now wraps it in `ConfigurationException`.
- [x] **DatabaseDataStorage** SQL hygiene (SonarCloud S2077 on every statement): the table name is interpolated once, in `Statements.of(dialect, table, index)`, and every execution site uses a precomputed constant. The identifier itself is still validated by `sanitizeTablePrefix` (`[A-Za-z0-9_]+`), which is what actually prevents injection — identifiers cannot be bound parameters.
- [x] **SonarCloud pass** (read through the SonarQube MCP server, 2026-09-20 evening): the 8 S2077 issues on `DatabaseDataStorage` closed with the `Statements` record; remaining reliability findings fixed (S3077 `volatile` references → `AtomicReference`, S2142 swallowed `InterruptedException` in `JDADiscordAPI.connect/disconnect`, S1192/S6353 in `DatabaseDataStorage`). The 8 security hotspots (all `githubactions:S7637`, "pin actions by SHA") were reviewed **Safe** — decision: keep version tags, Dependabot tracks them.
- [x] **Coverage measured wrong** on SonarCloud: JaCoCo reported per module, so `fluxcord-api` (only exercised by the core tests) showed 0 %. `fluxcord-core` now builds a `report-aggregate` (api + core) and `sonar.coverage.jacoco.xmlReportPaths` points at it. Second cause: `FluxcordRuntimeTest` booted the real engine, whose `ConsoleCommandService` read `System.in` and stole surefire's exit acknowledgement channel — the fork hung 30 s, was killed and never wrote `jacoco.exec` (0 bytes, no test failure). `FluxcordRuntime` has a constructor taking the console `InputStream`; tests pass `InputStream.nullInputStream()`.
- [x] **PluginManager.reloadPlugins** (full reload around a Discord reconnection; `PluginManagerTest` +2). Fix: the summary log used `{:.2f}` (not an SLF4J placeholder) so the numbers were shifted.
- [x] **Slash synchronization** (`CommandSynchronizationTest`, 3: global vs guild sets, unknown guilds skipped, boot sync then debounced runtime sync, no-op when disconnected). No bug.
- [x] **SlashCommandParser** (`SlashCommandParserTest`, 7). Fixes: `DiscordLocale.UNKNOWN.toLocale()` has language `"unknown"`, so the default-locale fallback never applied; an unresolvable MENTIONABLE yielded a raw string that `validateOptions` then rejected as "incorrect type" — now a clear `CommandParseException`.
- [x] **VersionCommand / ShutdownCommand** (`VersionAndShutdownCommandTest`, 3: console, non-operator refusal from Discord, operator shutdown reaching the handler). No bug.
- [x] `AbstractBinaryStorage.determineContentType`: 60-line if-chain → extension table; the static unbounded `contentTypeCache` (one entry per path ever asked) is gone. Dead classes `PermissionBuilder`, `PermissionChecker`, `PermissionImpl` deleted (0 users).
- [x] **Plugin-facing adapters** (`PluginAdaptersTest`, 5: `AbstractPlugin`, `PluginCommandAdapter` ownership checks, `PluginPermissionAdapter`, `PluginLanguageAdapter` namespacing, `ScopedBinaryStorage` on the file backend). **Storage factories** (`StorageFactoriesTest`, 5: FILE, DB through HikariCP on H2, `fallback` on/off for DB and S3). Also covered: per-guild prefix persistence, `CommandListener`, option groups/number bounds/file types in the slash mapper, `reply`/`replyEmbed`, the receive fan-out of `AudioPipeline`. No bug.
### Plugins (2026-09-22)

- [x] **music-plugin, first tests** (`TimeParserTest` 6, `PlaybackStateTest` 6, `TrackSchedulerTest` 16, `PlaylistManagerTest` 11, `PlaylistCommandTest` 10; module 0 % → 20.6 % lines). Fixes: `TimeParser` returned a negative position for a plain negative number and rejected a zero built from units (`/seek 0s` said "invalid time" while `/seek 0` worked); `PlaybackState.fromMap` fell back to volume 50 while a fresh player starts at 100.
- [x] **`/playlist` implemented** (decision 2026-09-22: the feature was announced in the README, the config (`playlists.*`) and the language files since the initial Codex port, but no command ever existed and `PlaylistManager` was dead code). `playlist <save|load|list|show|delete> [name] [personal|server]`, same "action option with choices" shape as the core `perm` command so it works from slash, text and console. Server-wide playlists need `music-plugin.admin`; personal ones are open to everyone (`music-plugin.playlist`).
  - `PlaylistManager` rewritten around the storage scopes: a personal playlist lives in the owner's user scope, a server one in the guild scope, both under `playlists.<name>`; nothing is cached in memory (the old version loaded *every* playlist of *every* owner at enable and rewrote all of them on each mutation). The `playlists.max_per_user/max_per_guild/max_tracks` limits are now enforced, and it takes a `PluginDataStorageAdapter` so it can be tested without a plugin.
  - `Playlist.fromMap` lost `createdAt` on every round-trip (a `// Use reflection or make fields package-private if needed` placeholder); `PlaylistTrack` is a record.
  - `MusicManager.loadPlaylist` resolves the stored URLs with `loadItemOrdered` (queue order preserved) and answers with one summary; the voice-join prologue it shares with `/play` is now `preparePlayback`.
- [x] **music-plugin simplified** (same pass): `MusicCommand` base class holds the guild lookup, the player lookup and `text(ctx, key, args)` — the 13 command classes repeated the same six-line prologue and `plugin.getLanguage().getString(ctx.getLocale(), ...)` everywhere; `MusicPlugin.musicCommand(name, configurer)` fills the name, description key and category of each registration.
- [x] **music-plugin coverage chantier** (decision 2026-09-22: go to ~70 %, leave the Discord-UI plumbing to the smoke run, like `JDADiscordAPI` in the core). Module **0 % → 65.5 %** lines, 150 tests.
  - Tier 1 — the 12 remaining commands (`PlaybackCommandsTest`, `QueueCommandsTest` on a shared `MusicCommandTestBase`), the three JDA listeners, `AudioPlayerSendHandler`, `SearchSourceManager`.
  - Tier 2 — `MusicPlayerTest` (15: state capture/restore, auto-leave, stop vs leave, destroy vs release), `TrackCodecTest`, `MusicManagerTest` (15: one player per guild, voice-update grace period, boot restore incl. expired/stale states, `loadPlaylist`), `MusicPluginTest` (10: permissions, the 13 registrations, listeners, config), `MusicAutocompleteTest` (7, providers reached through the options they are registered on).
  - Fixes found by these tests: **`/seek` rejected its own autocomplete suggestions** — the provider offers `1:30` and the option description advertises it, but `TimeParser` only understood `90s`/`1m30s`; clock notation is now parsed. `NowPlayingCommand` divided by the track duration without guarding zero and had a dead branch in its progress bar. `TrackCodec`'s javadoc claimed the position was not encoded (LavaPlayer 2.x carries it).
  - Test seams added: `MusicManager(plugin, AudioPlayerManager)` (the real one builds every YouTube/Spotify/Deezer client), `TestAudioSource` (LavaPlayer tracks that can be encoded — a Mockito mock cannot).
- [~] Left to the smoke run in `music-plugin`: only `MusicPlayerMessage` remains (196 lines at 25.8 %) — embed building and `RestAction` chains. `ButtonHandler`, `MusicModalListener` and `ModalCommandContext` were covered on 2026-09-26 and each yielded a real defect, so the "mock-heavy tests would mirror the code" judgement of 2026-09-22 was wrong for them.
- [x] **Examples and template** (2026-09-22): `plugin-example-commands` 0 % → 91.6 %, `plugin-example-audio` 0 % → 90.8 %, `plugin-template` 0 % → 73.8 % (63 tests). These modules are documentation that runs, so what they demonstrate has to work:
  - **The i18n of both the commands example and the template never resolved.** Their `lang/*.yml` wrapped everything in a redundant top-level block (`commands:`, `template:`) while the code asked for `ping.response` / `lifecycle.plugin_enabled`; every lookup missed and the raw key was shown to the user. The wrapper is gone (the adapter already namespaces by plugin id) and a `LanguageFilesTest` per module now checks that every key the code uses exists in both locales, with the same placeholders.
  - **The commands example hand-rolled what the framework does**: its `config.yml` declared cooldowns that nothing applied (it only *checked* a cooldown the core never set), and the admin command re-checked its permission in the executor. Both are declarative now (`.cooldown(...)`, `.permission(...)`), which deleted the dead `isOnCooldown` helper and the `commandexample.cooldown.bypass` permission that did nothing.
  - **`ExampleCommand` asked for `template.use` while the template registers `<plugin id>.use`**, so the example's permission check could never pass.
  - `plugin-example-*` declared `fluxcord-api` in `compile` scope, teaching the opposite of what a plugin must do (`provided`, see CLAUDE.md).
  - The audio example's two handlers are now top-level classes (`WavFileSendHandler`, `WavRecordingReceiveHandler`) instead of private inner ones: testable, and easier to copy. Their tests write and read real WAV files, pinning the byte-order flip (JDA gives big-endian PCM, WAV stores little-endian) and the silence padding of the last frame. Also removed: a `Math.random()` log sampler, a duplicated log line, dead TODOs, a legacy config key and a "persist the defaults" dance that `set()` never persisted anyway.
- [x] **`ai-audio-plugin`, common defects only** (2026-09-22, 0 % → 96.2 %, 10 tests). The plugin stays a skeleton — its TODOs and its actual purpose are the next session's subject — but the defects shared with the other generated plugins are fixed: the `aiaudio:` language wrapper, **four of its six config lookups pointing at keys absent from its own `config.yml`** (`ai.transcription_language`/`ai.tts_voice`/`ai.enable_voice_commands`/`ai.confidence_threshold` instead of `speech_recognition.*`, `text_to_speech.*`, `voice_commands.*`), the voice listener registered before the services it calls existed, a non-idempotent `onDisable`, a commented-out `@Command` annotation API that never existed, and the missing test setup. Settings are now an `AISettings` record read in one place.
- [~] **`ai-audio-plugin` becomes a real plugin** (2026-09-26). Decisions taken with Victor: build it in three steps — `/speak` first, then `/transcribe`, voice-to-voice last — on **OpenAI Whisper + TTS over HTTP now, Realtime later**; and it must run against **self-hosted models, local or remote** (a workstation on the LAN, or a service deployed beside the bot) as well as against the hosted API.
  - That last constraint set the architecture: everything speaks the **OpenAI audio HTTP API**, one `AiEndpoint` (base URL + key + model + timeout) per capability, and the two providers are behind `SpeechToText` / `TextToSpeech` interfaces. Local vs hosted vs in-cluster is then a URL, with no code path of its own — and the HTTP client tests, which run against a real loopback `HttpServer`, cover all three at once. No SDK, no native library: `java.net.http` plus the core's Gson.
  - Done: `/speak <text> [voice]`, `/silence`, `/transcribe <start|stop>`, `/forget <me|channel|server>`; `PcmAudio` (WAV parse/write, linear resampling, the big-endian flip JDA needs) and `PcmSendHandler` (a queue, so two `/speak` in a row are both heard and the voice connection is not dropped between two answers); `SpeechSegmenter` (per-speaker buffers closed on silence or on a maximum length, dropping anything too short to be speech) and `TranscriptionSession`; `ConversationMemory`.
  - **Transcription is per speaker**: the receive handler declares `canReceiveUser()`, so the core pipeline (`AudioPipeline.handleUserAudio`, which relays JDA's `UserAudio` untouched) delivers one call per speaker per packet and each utterance can be attributed to a name. Combined audio would give one stream and no way to tell who said what.
  - **Memory has three independent scales**, each sized in `config.yml` and each turned off with `0`: per voice channel (the conversation in progress), per server across its channels, and **per person across every server** (what somebody told the bot elsewhere, each turn keeping the server and channel it was said in). One storage key per turn, not a list per scope: `ScopedStorage.get` takes a `Class` and no type token, so a `List<Turn>` comes back as raw maps while a `Turn` round-trips through Gson as itself — and trimming a history becomes deleting the oldest keys.
  - Security note taken at design time: a Discord nickname is chosen by its owner, so `Turn` and `ConversationContext` keep names and text in their own fields and nothing builds a prompt by concatenation.
  - Also removed: `AudioAnalysisService` (~20 lines of TODOs for "emotion recognition" and "content analysis" that nothing planned needs — say so if it should come back), the `analyze` and `voicecommands` permissions for features that did not exist, **okhttp and a relocated Gson that no line of code ever used** (gson is in `CORE_PACKAGES`, so a bundled copy is never the one loaded — it is `provided` now), and with them the whole shade configuration, since nothing is bundled any more. The README documented 8 commands that did not exist and configuration keys that had already been deleted; it is rewritten on what is there, with the self-hosted recipe and the gfx1031 ROCm note.
  - Module at **94.4 %** of 750 lines, 111 tests (it had 10 tests for 60 lines). The command tests assert the order of the checks, not only their outcome — an empty text is refused before the bot joins a channel, a missing key before any request leaves. The service tests walk the whole transcription chain (audio on the JDA thread → silence noticed by the scheduler → request on a worker → name resolved → message posted and turn recorded) with an injected clock and no network, and pin that a failing provider does not kill the poller: a scheduled task that throws is never run again.
- [x] **i18n audit across every module** (2026-09-26), started by one warning in a smoke run and finished repository-wide, per Victor's rule: when a bug is found, look for it everywhere.
  - `PluginLanguageAdapter` (api) now **warns on every lookup that misses** and its debug line prints the resolved value. It used to print the namespaced key with an arrow as if that were the result — which reads like a failure even on success, and did send a reader hunting a bug that was not there. The fix is in the api, so every plugin benefits.
  - That warning immediately exposed two live misses. `music.command.loop.mode.off` was translated in both locales and never resolved: snakeyaml implements YAML 1.1, where an unquoted **`off` key** is the boolean `false`, so it flattened to `...mode.false` and `/loop` labelled one of its choices with a raw key. `music.error.guild_only` simply did not exist, so every music command run in a DM answered with its key — introduced with `MusicCommand` on 2026-09-22.
  - A scan of every `lang/*.yml` then found **24 formatted values carrying a lone apostrophe**, including `fluxcord-core`'s own `fr-FR.yml` (cooldown, execution error) and eight strings in the template shaped `'{0}'`, which MessageFormat renders as a *literal* `{0}` — the user saw the placeholder instead of the value. All 24 fixed.
  - `music-plugin` was the only module with `lang/*.yml` and no `LanguageFilesTest`, which is precisely why it held both misses. It has one now, and rather than a hand-kept key list it **scans its own sources** for literals passed to `text(` / `getString(` — that is what caught `guild_only`. The three shared guards (keys are strings, locales in step, no lone apostrophe in a formatted value) were added to the examples, the template and ai-audio, and `fluxcord-core` gained `CoreLanguageResourcesTest` for its own files, which no test covered before.
  - 724 tests across the reactor, all green.
- [x] **Config and language keys, swept in both directions across every module** (2026-09-26, at Victor's request to clean up keys that are no longer used).
  - **Read but not shipped** (a setting nobody can discover), 3 found and documented: `data.storage.file.folder` — where the FILE backend writes everything, while its binary counterpart *was* documented — `data.storage.file.debounce_ms`, and the template's `integration.check_other_plugins`.
  - **Shipped but never read** (it looks like a setting and changes nothing), 60 found and removed: 18 in `plugin-template`, 17 in the commands example, 4 in `music-plugin` (a vote-skip feature that does not exist, and YouTube `email`/`password` a hoster would have filled in for nothing), 3 in the audio example, and `commands.cooldown` in the core — which its own migration wrote into every user's `config.yml` while no code path read it, so the migration lost that step too.
  - Same sweep on `lang/*.yml`: the **audio example still carried the `audioexample:` wrapper** (missed when the other two were fixed on 2026-09-22) and all 14 of its strings were unused, since the plugin has no user-facing text — folder removed. The template declared 38 strings for 6 used, the commands example 34 for 18, and the core had a **duplicate `success:` key** in one block where the second silently won. All trimmed to what the code looks up.
  - `ConfigurationKeysTest` now enforces both directions from the sources, in `fluxcord-core` and in `plugin-template` so every new plugin inherits it. The six modules match exactly, in both directions, as of this commit. 728 tests green.
  - Deliberately kept: `permissions.error.denied` / `.not_found` in the core's lang files, unused by production code but used as `SimpleLanguageManagerTest`'s cascade fixtures.
- [x] **Shared audio primitives promoted to `fluxcord-api`** (2026-09-26, Victor: put what is commonly useful in common). `PcmAudio` (RIFF/WAVE in and out, linear resampling, the big-endian flip JDA needs) and `PcmSendHandler` (a queue of clips, 20 ms frames, silence padding) moved from `ai-audio-plugin` to `fr.farmvivi.fluxcord.api.audio`, with their 21 tests — which are also the **api module's first tests**, since it had neither tests nor test dependencies. `plugin-example-audio`'s receive handler dropped its hand-rolled byte swap for `PcmAudio.fromBigEndian`. Its `WavFileSendHandler` is deliberately kept: it streams a file frame by frame without loading it, which is a different job from queueing clips already in memory.
- [x] **Transcription straight from a multimodal model** (2026-09-26), found by measuring Victor's Ollama server rather than assuming.
  - Measured there, warmed and with `think:false`: **Gemma 4 E4B 0.08 s to first token, 0.27 s total, 79 tok/s**; Qwen 3.5 9B 0.17 s / 53 tok/s; Gemma 4 12B 0.19 s / 41 tok/s. Phi-4, Qwen 3 14B and Granite 4.1 8B sit at 4-11 tok/s — partly on CPU, and unusable for conversation (8-10 s for one sentence).
  - `/api/show` reports capabilities: **Gemma 4 lists `audio`** (plus vision, tools, thinking); Qwen 3.5 does not. Tested end to end with a French sentence synthesised by Windows SAPI: Gemma 4 E4B transcribed it **exactly**, in 0.57 s. So one model can cover transcription, reasoning and tool calls in a single round trip, and no speech service is needed beside it.
  - Two traps, now pinned by tests: the audio travels in the **`images`** field (an `audio` field is accepted and silently ignored — the model then answers that it received nothing), and `think` must be off.
  - `OllamaSpeechToText` is a second implementation of the existing `SpeechToText` interface, selected by `speech_to_text.api: OPENAI | OLLAMA`. The interface earned its keep: nothing else changed.
  - Incidental validation: the SAPI file has an 18-byte `fmt` chunk rather than 16, and `PcmAudio.fromWav` reads it correctly — a real-world file the parser had never seen.
  - Still missing for `/speak`: **Ollama does not synthesise speech.** A TTS server is needed (Piper or Kokoro-FastAPI, both fine on CPU), or OpenAI.
- [~] **Coverage chantier, round 2** (2026-09-26, Victor: raise the project's coverage). `fluxcord-api` **18.5 % -> 71.5 %** (99 tests), project total 76.7 % -> **80.8 %**.
  - The api was the biggest and cleanest gap: its classes are pure logic, and per-module JaCoCo only credits a module's own tests, so everything the api does was uncounted even where core tests exercised it. Covered: the scope formats (`StorageKey`, `BinaryStorageKey` including path normalisation), `ScopedStorage` and `ScopedBinaryStorage` prefixing, the five plugin adapters, `AbstractPlugin`, `CommandOption.isValid` and the event contracts that listeners actually interfere with (cancellation, value and result overrides).
  - The tests are written around what is load-bearing rather than around getters: scope strings are a compatibility surface (rename a separator and every stored prefix, permission and playlist becomes unreachable), and `isValid`'s order of checks is pinned because choices short-circuit the range and length bounds entirely.
  - **Two build facts uncovered on the way.** The api needed `mockito-junit-jupiter` like every other module; and it needs the **subclass mock maker** (`src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker`), because the inline maker's Byte Buddy agent and JaCoCo both want to transform this module's own classes — the very ones its tests mock — and on JDK 25 the agent self-attach fails outright. Other modules are unaffected: the types they mock come from this jar and are not instrumented there. Consequence: no test here may mock a final class, so `ScopedStorage` and `ScopedBinaryStorage` are used for real over a mocked backend — which checks the actual prefixing rather than a mock's identity.
  - Then `music-plugin` **65.4 % -> 74.6 %**: `ButtonHandler` and `MusicModalListener`, the two classes a user actually touches, which the earlier passes had left to the smoke run. The button tests assert that an action is refused *before* it reaches the player and that a refusal never edits the message — a permission check running after its side effect would look identical in a screenshot. The modal tests pin the query mapping: free text becomes a YouTube search whatever the provider was picked (source managers want URLs), while a pasted link is never rewritten. `ButtonHandler.ProviderOptions` went from `private` to package-private to be tested on its own — eight configuration branches and credential checks, unreachable otherwise except through JDA's component tree; that is audit checklist item 8 applied to our own code.
  - Noted, not changed: `loop`, `loopqueue` and `shuffle` check **no permission at all** while `skip` requires `music-plugin.skip`. Deliberate or not, it is now pinned by a test, so changing it is a decision rather than a surprise — **ask Victor**.
  - Project total after this pass: **83.1 %** (846 tests).
  - Remaining, by size: `MusicPlayerMessage` 209 lines at 24 %, `JDADiscordAPI` 148 at 0 %, `MusicManager`'s anonymous `AudioLoadResultHandler` 52, `ModalCommandContext` 47, `Fluxcord` 44, `music-plugin` 65.4 % (the JDA-facing UI: `MusicPlayerMessage` 212 lines, `ButtonHandler` 112+38, `MusicModalListener` 87, `ModalCommandContext` 53), `JDADiscordAPI` 148 lines at 0 %, `Fluxcord` 44, `plugin-template` 73.8 %, and ~200 lines of api event/exception carriers.
- [x] **Music permissions standardised, and a bypass closed** (2026-09-26). Asked to pick the right nodes and standardise, the answer turned out to be *make the code do what the README already said*: that table documented a permission per command and **not one of them was enforced** — only the buttons checked anything.
  - Consequence before the fix: `music-plugin.volume` defaults to `OP` and the volume button honoured it, while `/volume` declared nothing, so the restriction was bypassable by typing the command instead of clicking the button. Same for `/skip` against the skip button.
  - Now one node per *action*, whatever the input method, exactly as the README documents: `play` for play/pause/stop/seek, `skip` for skip, `queue` for queue/nowplaying/loop/shuffle/remove, `volume` for volume, `admin` for clear, `playlist` for playlist. The `queue` permission's description said "viewing the queue" while the README used it for reordering too; it now says "viewing and reordering". The README's node names were `music.*` and are corrected to the registered `music-plugin.*`.
  - `MusicPluginTest.everyCommandDeclaresThePermissionItsButtonChecks` pins the whole table, and `ButtonHandlerTest` pins the button side, so the two cannot drift apart again.
  - Visible change for users: the three mode buttons (loop, loop-queue, shuffle) now require `music-plugin.queue`, which defaults to TRUE — so nobody is locked out today, but it became revocable.
- [x] **One progress bar instead of two** (2026-09-26), `util/ProgressBar`. The copy in `MusicPlayerMessage` still had both defects the `NowPlayingCommand` copy had been fixed for on 2026-09-22 — a lesson in checking *every* occurrence: it **divided by the duration with no guard** (LavaPlayer reports no usable duration for a live stream, so the refresh loop threw) and drew the played and remaining halves with the same character, so the bar showed nothing but its knob. The shared version clamps the knob inside the bar (at `position == duration` the old arithmetic put it one past the end, leaving a bar with no knob at all) and is tested on its own, including the `Long.MAX_VALUE` unknown-duration case that overflows a naive multiplication.
- [x] **`music-plugin` coverage, round 3** (2026-09-26): `ModalCommandContext` (the adapter that lets a modal submission reuse the ordinary command code) and `MusicManager`'s load outcomes. The context's whole job is one decision repeated per reply shape — an interaction may be acknowledged once, so a reply goes through `event.reply` the first time and through the webhook hook afterwards; getting it wrong throws inside a callback where the user only sees "the application did not respond". The load tests capture the `AudioLoadResultHandler` and drive each outcome: a search result plays its first hit instead of queueing fifty, a real playlist is queued whole and announced once, nothing found and a provider failure both say so.
- [x] **Sonar cleanup** (2026-09-26, after the gate went green on coverage and red on reliability instead).
  - The two remaining reliability findings were my own regexes, and the second one taught something: replacing `[A-Za-z_]*[Cc]onfig` with `(\w+)\s*(?:\(\))?\s*\.` did not help, because `\s*(?:…)?\s*` collapses into `\s*\s*` when the optional group is absent — the same ambiguous backtracking. The space now lives inside the group.
  - `CoreSettings.Database.maxPoolSize`/`autoCommit` were boxed and nullable so the pool could substitute a default for null — the nullability bought nothing and invited an unboxing NPE. They are `int`/`boolean` with the defaults applied at read time, and the two keys are finally documented in `config.yml` (my own `ConfigurationKeysTest` caught their absence the moment they started using the two-argument getters).
  - `PermCommand` went from a single switch at cognitive complexity **32** to one method per action plus a `Request` record — for a command that grants and revokes permissions, that is exactly where nobody should be guessing. Five duplicated literals became constants in the process.
  - The permission nodes of `music-plugin` and `ai-audio-plugin` are now named constants shared by the registration, the commands and the buttons. `ButtonHandler` no longer carries `music.<node>` literals whose prefix it rewrote at every call: it names the short node and asks the plugin to qualify it, so a button cannot check a name the plugin never registered.
  - **Eight copies of the same `StubPlugin`** in the core tests became one in `core/testing`, which also removed the twenty "empty method" warnings. `SimpleEventManagerTest` keeps a local subclass, and now says why: the manager auto-registers the event types of the *plugin class's own package*, so that stub has to live beside the test events — deduplicating it blindly broke that test, which is the kind of coupling worth a comment.
  - Also: `PcmAudio.fromWav` split into header/format/data readers (complexity 22), a zero-width space escaped, `Collections.reverse` replaced by `reversed()`.
  - **Left alone on purpose**: the cognitive complexity of `PositionalArguments` (24), `CommandExecutor` (19), `ConsoleReplyTarget` (19), `SlashCommandDataMapper` (19), `SimpleLanguageManager` (18, plan item L1) and `AudioPipeline` (16). These are pre-existing core paths whose refactor is a chantier of its own, not a cleanup — L1 and C1/C2 already describe them.
- [x] **Coverage, round 3: the non-JDA gaps** (2026-09-26). Project **84.5 % -> 85.7 %**, 922 tests. `fluxcord-api` 71.5 % -> **81.4 %**, `plugin-template` 73.8 % -> **89.3 %**.
  - api: the forwarding half of `PluginCommandAdapter`, the `CommandException` family (a parse failure that forgets which parameter it was about leaves the user with "invalid value"), and the audio and binary-storage events.
  - The template's whole lifecycle, phase by phase, written as statements about the contract it teaches: permissions in `onPreEnable`, commands and listeners in `onEnable`, cross-plugin work in `onPostEnable`, every feature switchable off without leaving a half-built plugin.
  - **Two defects in the template.** `checkPluginIntegrations` asked `getPluginLoader().getPlugin("MusicPlugin")` while the registry is keyed by plugin **id** (`music-plugin`), so that branch could never fire — and the template was teaching the wrong key. And **I had broken its feature defaults myself** when rewriting its `config.yml` this morning: the original shipped `false` everywhere, deliberately, so that a fresh copy registers nothing on somebody's bot. I nearly "fixed" the code to match my own mistake; both are back to `false`, and the tests now opt in explicitly, which is what a plugin author does.
  - `FileBinaryStorage`'s error paths and its one-off layout migration (`user:1/` -> `user/1/`). The migration tests are skipped on Windows and run on the Linux CI, since only a filesystem allowing a colon can hold the old folder name.
  - Remaining, and deliberately so: ~480 lines of JDA plumbing — `MusicPlayerMessage` (196, embeds and `RestAction` chains), `JDADiscordAPI` (148 at 0 %, would need a fake `JDABuilder`; exercised by every smoke run), `Fluxcord.main` (44), the voice glue in `MusicPlayer` and the network clients of `AudioPlayerManager`. Plus a long tail of error branches spread thin.
- [x] **Adaptive persona and mood** (2026-09-26), the feature Victor asked for: a personality that adjusts per server, per channel and per person, and a mood that drifts with the conversation.
  - `Persona` (name, traits, tone, language, instructions) is resolved base -> server -> channel, each level stating **only what differs**. The type deliberately substitutes no defaults: it doubles as a partial override, and defaulting a blank name there would make every override rename the bot. The defaults live where the base is read.
  - `Mood` is two bounded axes (energy, warmth) rather than a named emotion, because a name cannot be moved a little and a conversation moves a mood gradually. It **fades back to neutral**, halving every 20 minutes, computed from the elapsed time on read — so a quiet channel calms down with nothing running, and it is testable without waiting. Kept per voice channel: a mood belongs to a conversation, not to a community.
  - Adaptation to people: `PersonaSnapshot` grades each participant STRANGER / KNOWN / REGULAR from their remembered turns, counted on their **cross-server** history, which is what the per-person memory scope was for.
  - What moves the mood today: each transcribed sentence raises the energy by `persona.mood.energy_per_turn`. Only the energy — whether a conversation is warm or cold cannot be told from the fact that it is happening, and guessing it from keywords would be worse than not guessing. The voice-to-voice step will be able to report a real delta.
  - `/persona show|set|reset [field] [value] [scope]` makes it usable before the model step exists. Showing is open; changing needs `ai-audio-plugin.admin`, and one call edits one field at one level.
  - 65 tests for the persona package (`Mood` and `Persona` at 100 %, module 93.1 %). Everything is pure logic: no network, no JDA, no clock — the time is passed in.
- [ ] **Web search for `ai-audio-plugin`** — decided 2026-09-26, not built yet. Backend: **SearxNG**, which Victor already self-hosts, so queries never leave his network. Shape: a `WebSearch` interface with one implementation per API, selected by config exactly like `speech_to_text.api` — SearxNG first, with room for Ollama's own `/api/web_search`, an OpenAI-style endpoint and any OpenAPI-described service, since the repository is public and other people's deployments will differ. The model reaches it as a **tool call** (`tools` in `/api/chat`; Gemma 4 and Qwen 3.5 both declare `tools`), so the plugin runs the search and hands back results. **Security requirement, decided up front**: a fetched page is untrusted input — results must be passed as delimited data and never as instructions, the same rule already applied to Discord nicknames. Worth stating in the README too: with any hosted backend, what people say in a voice channel would leave the network.
- [ ] **Next on `ai-audio-plugin`**: the voice-to-voice turn — the LLM step between ears and voice, on the same OpenAI-compatible surface (so Ollama serves it locally or remotely unchanged), with the three memory lookups exposed as **tool calls** so the model asks for the context it needs. Victor asked about MCP for this: function calling is the direct path since the memory is in the same JVM, and MCP only becomes relevant to let *other* agents read the bot's memory.
- [ ] The audit checklist built from all these plugins lives in `.claude/skills/fluxcord-plugin-dev/SKILL.md` ("Audit checklist for a generated plugin"): language wrapper, config keys read vs declared, framework features reimplemented by hand, permission names, lifecycle ordering, dependency scopes, dead/lying code, untestable inner classes, missing test setup.
- [ ] In `music-plugin` the untested mass is now `MusicPlayerMessage` alone, plus the network-facing parts of `AudioPlayerManager` (source clients) and the JDA voice glue in `MusicPlayer`.

- [ ] Left uncovered on purpose: `JDADiscordAPI` (would need a fake `JDABuilder`; exercised by every smoke run), `Fluxcord.main`. Aggregate after this pass: core 87.1 %, api 82.5 % (lines). Decision (2026-09-20): nothing is excluded from the quality gate — the plugins (`music-plugin` ~2 000 lines, examples, template, `ai-audio-plugin`) are the remaining 0 % and need their own test chantier.
